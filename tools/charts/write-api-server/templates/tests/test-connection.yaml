apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "write-api-server.fullname" . }}-test-connection"
  labels:
    {{- include "write-api-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: [ 'wget' ]
      args: [ '{{ include "write-api-server.fullname" . }}:{{ .Values.service.port }}' ]
  restartPolicy: Never
