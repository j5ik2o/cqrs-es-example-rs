// use refinery::error::Kind::Connection;

// mod embedded {
//   use refinery::embed_migrations;
//
//   embed_migrations!("query/refinery/migrations");
// }

fn main() {
  // let mut conn = Connection::open_in_memory().unwrap();
  // embedded::migrations::runner().run(&mut conn).unwrap();
}
